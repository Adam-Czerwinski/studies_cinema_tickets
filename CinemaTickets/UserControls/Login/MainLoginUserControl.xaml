<UserControl x:Class="CinemaTickets.Pages.Login.MainLoginUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CinemaTickets.Pages.Login"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Height="20" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Button x:Name="ClientButton" Content="Klient" Width="100" Click="OnClientClick"/>
            <Button x:Name="EmployeeButton" Content="Pracownik" Width="100" Click="OnEmployeeClick"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Grid.Column="0" x:Name="LoginContentControl" />
    </Grid>
</UserControl>
